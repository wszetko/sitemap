build:
  nodes:
    analysis:
      project_setup:
        override:
          - 'true'
      environment:
        php:
          version: 7.3
      tests:
        override:
          -
            command: 'composer phpunit'
            coverage:
              file: 'build/clover.xml'
              format: 'clover'
checks:
  php:
    code_rating: true
    duplication: true
coding_style:
  php:
    spaces:
      around_operators:
        concatenation: true
filter:
  excluded_paths: [tests/*]
